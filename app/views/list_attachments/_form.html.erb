<%= image_tag @list_attachment.avatar %>
<%= form_for(@list_attachment) do |f| %>
  <% if @list_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@list_attachment.errors.count, "error") %> prohibited this list_attachment from being saved:</h2>

      <ul>
      <% @list_attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :list_id %><br>
    <%= f.number_field :list_id %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.text_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
