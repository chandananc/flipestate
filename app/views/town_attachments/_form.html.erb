<%= image_tag @town_attachment.image %>
<%= form_for(@town_attachment) do |f| %>

  <% if @town_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@town_attachment.errors.count, "error") %> prohibited this town_attachment from being saved:</h2>

      <ul>
      <% @town_attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :town_id %><br>
    <%= f.number_field :town_id %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.text_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
